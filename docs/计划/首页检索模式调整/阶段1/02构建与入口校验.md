# 阶段1 02：构建与入口校验

## 目标

确认首页改造没有破坏现有前端构建和主要入口。

## 校验项

1. 执行 `npm.cmd run build`。
2. 检查首页仍由 `frontend/src/app/page.tsx` 渲染。
3. 检查导航仍可进入聊天页、研究方向和论文写作。
4. 确认 `pipeline` 页面未被删除，仅不作为首页主要入口使用。

## 通过标准

- 构建通过。
- 首页搜索不再跳转聊天页。
- References 面板可展示检索返回的文献来源。

