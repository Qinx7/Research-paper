# 阶段 3：研究方向仪表盘对齐

## 目标

把研究方向页进一步对齐截图 2：左侧是方向候选列表，右侧是当前方向的趋势、热度、代表学者、研究空白与机会。

## 重点

- 左侧方向卡片显示方向标题、领域、相关论文数、热度或评分。
- 右侧顶部显示方向名称、标签、热度。
- 统计卡显示相关论文、年增长率、热度排名。
- 趋势图先用轻量 CSS 条形图，不新增图表依赖。
- 研究空白与机会使用真实 `risks`、`research_gaps` 或后端已有字段。

## 不做

- 不伪造真实论文数量或增长率。
- 若后端没有对应数据，显示“暂无数据”或使用已有评分字段近似展示。
